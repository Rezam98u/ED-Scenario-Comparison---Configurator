# Copy to server/.env and adjust for your machine.
# Example matches docker-compose.yml at repo root.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/energy_dashboard?schema=public"

# Port for the Express API (Vite proxies /api here in development)
PORT=4000
