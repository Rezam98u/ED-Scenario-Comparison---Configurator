# Optional: full URL of the API when the frontend is not served behind the same origin
# (e.g. production). Leave unset in local dev — Vite proxies /api to the Express server.
# VITE_API_URL=http://localhost:4000
